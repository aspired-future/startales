#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running lint-staged..."
npx lint-staged
node scripts/secret-scan.js || true
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "Running lint and unit tests..."
npm run -s lint && npm run -s test:unit

